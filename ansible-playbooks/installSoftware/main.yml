---
- hosts: webserver
  remote_user: ubuntu
  become: yes
  become_method: sudo
  vars:
    temp_folder: /tmp
  tasks:    
    # Install prerequisites    
    - include: tasks/prerequisites.yml  
    # Install MongoDB    
    # - include: tasks/mongodb.yml
    # Install Node.js    
    - include: tasks/nodejs.yml
    # Install Ngnix    
    - include: tasks/ngnix.yml
